apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: harbor-ext
spec:
  rules:
  - host: "harbor.talos-private.lan"
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: harbor-2
            port:
              number: 443
