apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: harbor-ext
spec:
  rules:
  - http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: harbor
            port:
              number: 80
