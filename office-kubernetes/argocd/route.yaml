apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: argocd
spec:
  parentRefs:
  - name: web
    namespace: argocd
  hostnames:
  - "argocd.k8s.local-k8s.tech"
  rules:
  - backendRefs:
    - name: argocd-server
      port: 80
