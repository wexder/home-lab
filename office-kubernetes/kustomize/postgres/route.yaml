apiVersion: gateway.networking.k8s.io/v1alpha2
kind: TCPRoute
metadata:
  name: main-pool-db
spec:
  parentRefs:
  - name: postgres
    sectionName: foo
  rules:
  - backendRefs:
    - name: main-psql-pool
      port: 5432
