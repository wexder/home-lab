apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: odoo

resources:
- namespace.yaml
- pvc.yaml
- deployment.yaml
- service.yaml
- route.yaml
- odoo-secret.yaml

configMapGenerator:
  - name: odoo-config
    files:
      - config/odoo.conf
