apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: glance

resources:
- namespace.yaml
- deployment.yaml
- service.yaml
- route.yaml

configMapGenerator:
  - name: glance-config
    files:
      - config/glance.yaml
      - config/home.yaml
