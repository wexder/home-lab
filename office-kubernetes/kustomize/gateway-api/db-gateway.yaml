apiVersion: gateway.networking.k8s.io/v1
kind: Gateway
metadata:
  name: database
spec:
  gatewayClassName: cilium
  listeners:
    - name: postgres
      protocol: TCP
      port: 5432
      allowedRoutes:
        kinds:
        - kind: TCPRoute
