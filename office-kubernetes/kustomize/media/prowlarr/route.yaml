apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: prowlarr
spec:
  parentRefs:
  - name: web
    namespace: gateway-system
  hostnames:
    - "prowlarr.k8s.local-k8s.tech"
  rules:
    - backendRefs:
      - name: prowlarr
        port: 80
