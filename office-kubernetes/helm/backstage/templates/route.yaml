apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: backstage
spec:
  parentRefs:
  - name: web
    namespace: gateway-system
  hostnames:
    - "backstage.k8s.local-k8s.tech"
  rules:
    - backendRefs:
      - name: backstage 
        port: 7007
