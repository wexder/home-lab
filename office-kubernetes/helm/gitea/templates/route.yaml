apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: gitea
spec:
  parentRefs:
  - name: web
    namespace: gateway-system
  hostnames:
    - "gitea.k8s.local-k8s.tech"
  rules:
    - backendRefs:
      - name: gitea-http
        port: 3000
