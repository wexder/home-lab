apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: garage
spec:
  parentRefs:
  - name: web
    namespace: gateway-system
  hostnames:
    - "garage.k8s.local-k8s.tech"
  rules:
    - backendRefs:
      - name: garage
        port: 3902
