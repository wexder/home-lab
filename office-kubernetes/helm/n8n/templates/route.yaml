apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: n8n
spec:
  parentRefs:
  - name: web
    namespace: gateway-system
  hostnames:
    - "n8n.k8s.local-k8s.tech"
  rules:
    - backendRefs:
      - name: n8n
        port: 80
