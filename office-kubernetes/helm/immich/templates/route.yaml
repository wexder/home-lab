apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: immich
spec:
  parentRefs:
  - name: web
    namespace: gateway-system
  hostnames:
    - "immich.k8s.local-k8s.tech"
  rules:
    - backendRefs:
      - name: immich-server
        port: 2283
