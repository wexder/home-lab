apiVersion: v1
kind: Service
metadata:
  name: valkey-cluster-lb
  namespace: valkey
spec:
  ports:
    - name: valkey
      port: 6379
      protocol: TCP
      targetPort: 6379
  selector:
    valkey.io/cluster: valkey-cluster
  type: LoadBalancer
