apiVersion: v1
kind: Service
metadata:
  name: jellyfin
  labels:
    app: jellyfin
spec:
  ports:
    - port: 9696
      name: default
      targetPort: 9696
      protocol: TCP
    - port: 80
      name: tailscale
      targetPort: 8920
      protocol: TCP
  selector:
    app: jellyfin
