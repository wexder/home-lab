apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: filebrowser-tailscale
spec:
  defaultBackend:
    service:
      name: filebrowser
      port:
        name: tailscale
  ingressClassName: tailscale
  tls:
  - hosts:
    - filestash
