apiVersion: v1
kind: Service
metadata:
  name: filebrowser
  labels:
    app: filebrowser
spec:
  ports:
    - port: 8080
      name: default
      targetPort: 8080
      protocol: TCP
    - port: 80
      name: tailscale
      targetPort: 8080
      protocol: TCP
  selector:
    app: filebrowser
