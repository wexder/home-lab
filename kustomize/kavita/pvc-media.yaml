apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: kavita-media-pv
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: ""
  resources:
    requests:
      storage: 100Gi
  volumeName: kavita-media-pv
