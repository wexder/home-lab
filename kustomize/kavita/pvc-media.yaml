apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: kavita-media
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: ""
  resources:
    requests:
      storage: 100Gi
  volumeName: kavita-media-pv
